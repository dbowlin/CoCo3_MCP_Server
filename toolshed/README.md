# ToolShed `decb` (optional)

Only needed for `coco_build_disk`.

1. Install ToolShed so you have `decb.exe`.
2. Copy or link it here as:
  ```text
   toolshed/decb.exe
  ```
3. `MCP/.env` already points at `../toolshed/decb.exe`.

